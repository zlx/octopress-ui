require "bundler/gem_tasks"

desc 'install octopress-ui'
namespace :octopressui do
  task :install => :environment do
    #FileUtils.cp 'lib/octopress-ui/ui.rb',
    p $root rescue nil
    p __FILE__ rescue nil
  end
end
